use yew::prelude::*;

// Not sure if this is necessary at all but here it is

#[function_component(Divider)]
pub fn divider() -> Html {
    html! {
        <div class="divider">
        </div>
    }
}
